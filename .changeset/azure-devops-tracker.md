---
"mattpocock-skills": minor
---

Add first-class Azure DevOps support to `setup-matt-pocock-skills`.

- Configure an Azure DevOps organization and project explicitly.
- Create Azure Boards `Feature` parents with child `Task` tickets and map triage roles to tags.
- Support Azure Repos pull requests as an optional triage surface.
- Use native Child and Predecessor links for wayfinder maps and blocking edges.
- Recognize Azure Boards `AB#<id>` references when locating specs.
